package com.dearnik1.learnjava.basics.circle;

public class Circle {
	/*
		Implement class "Circle" with fields (class should have appropriate constructor):
			x,y - coordinates of the center of a circle;
			radius - radius of a circle;
			
		and methods:
			void move(int dx, int dy) - moves a circle by dx and dy;
			boolean contains(inx x, int y) - checks if a given point located inside the circle;
			boolean contains(Circle otherCircle) - checks if a other circle located inside the circle;
			void printCircle() - display circle parameters in the console (format: "x: 13, y: 24, radius: 77");
	*/
}
